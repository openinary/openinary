/_next/static/*
  Cache-Control: public,max-age=31536000,immutable

# The shadcn registry, copied from /r at build time (prebuild). Short TTL, not
# immutable: the block's source changes and consumers re-fetch by the same URL.
/r/*
  Cache-Control: public,max-age=300
  Content-Type: application/json
