---
"@openinary/ui": patch
---

MediaGrid: new `emptyActions` and `emptyFooter` props to replace the empty-state CTAs (defaults unchanged). Long filenames no longer overflow: the upload dialog's file list stays inside the dialog (grid min-width fix) and toast titles truncate instead of stretching the pill across the viewport.
